<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Our Community</title>
  <link rel="stylesheet" href="/stylesheets/usersindx.css">
</head>
<body>
  <div class="user-content">
    <h1>Our Community</h1>
    <ul>
      <% users.forEach(user => { %>
        <li><a href="/users/<%= user._id %>"><%= user.username %></a></li>
      <% }); %>
    </ul>
  </div>
</body>
</html>